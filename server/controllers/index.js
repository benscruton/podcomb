const authController = require("./auth.controller");
const combController = require("./comb.controller");
const podcastController = require("./podcast.controller");

module.exports = {
  authController,
  combController,
  podcastController 
};